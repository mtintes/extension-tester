name: test

on:
    workflow_dispatch:
        inputs:
            test:
                description: 'options'
                required: true

jobs:
    test:
      name: test job
      runs-on: ubuntu-latest
      steps:
        - name: test
          run: echo "test"
